server {
        listen 80 default_server;
        listen [::]:80 default_server;
        root /ipspace_bucket;
        server_name _;
        location / {
                try_files $uri $uri/ =404;
        }
}
